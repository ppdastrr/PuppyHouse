<UserControl x:Class="KP_4_PuppyHouse1.Pagess.PostavkaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:Inter="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <!-- Заголовок -->
            <RowDefinition Height="auto"/>
            <!-- Панель управления -->
            <RowDefinition Height="auto"/>
            <!-- Фильтры и поиск -->
            <RowDefinition Height="*"/>
            <!-- Таблица с данными -->
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Поставка" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
                   HorizontalAlignment="Left" Foreground="#121518"/>

        <!-- Панель управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button x:Name="AddPostavka_Btn" Margin="10 10 0 0" Width="80" Style="{StaticResource addButton}" Command="{Binding AddProductCommand}" Click="AddPostavka_Btn_Click_1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="➕" FontSize="18"/>
                </StackPanel>
            </Button>

        </StackPanel>

        <!-- Фильтры и поиск -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 15 0 0" HorizontalAlignment="Stretch">
            <!-- Поле для поиска -->
            <TextBox x:Name="SearchBox" Width="200" Margin="10 0 10 0" VerticalAlignment="Center"
                     Style="{StaticResource textBoxSearch}" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TextChanged="SearchBox_TextChanged_1" 
                     />
        </StackPanel>

        <!-- Таблица с данными -->
        <DataGrid x:Name="dataGrid" Style="{DynamicResource DataGridStyle}" 
                  CellStyle="{DynamicResource DataGridCellStyle}" 
                  RowStyle="{DynamicResource DataGridRowStyle}" 
                  ColumnHeaderStyle="{StaticResource TransparentHeaderStyle}"
                  Grid.Row="3" ItemsSource="{Binding FilteredProducts}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Товар" IsReadOnly="True" Width="*" Binding="{Binding Tovar.Name}"/>
                <DataGridTextColumn Header="Количество" IsReadOnly="True" Width="*" Binding="{Binding Count}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="Поиск:" HorizontalAlignment="Left" Margin="20,33,0,0" Grid.Row="1" VerticalAlignment="Top" Grid.RowSpan="2"/>
    </Grid>
</UserControl>