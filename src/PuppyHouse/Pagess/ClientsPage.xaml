<UserControl x:Class="KP_4_PuppyHouse1.Pagess.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KP_4_PuppyHouse1.Pagess"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <!-- Заголовок -->
            <RowDefinition Height="auto"/>
            <!-- Панель управления -->
            <RowDefinition Height="auto"/>
            <!-- Фильтры и поиск -->
            <RowDefinition Height="*"/>
            <!-- Таблица с данными -->
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Клиенты" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
                HorizontalAlignment="Left" Foreground="#121518"/>

        <!-- Панель управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Width="187">
            <Button Margin="10 10 0 0" Width="80" Style="{StaticResource addButton}" Command="{Binding AddClientCommand}" FontSize="18">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="➕"/>
                </StackPanel>
            </Button>

            <Button Margin="10 10 0 0" Style="{StaticResource addButton3}" Command="{Binding DeleteSelectedCommand}" Width="80" Height="35">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🖉" FontSize="20"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Фильтры и поиск -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 15 0 0" HorizontalAlignment="Stretch">
            <!-- Поле для поиска -->
            <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" Width="200" Margin="10 0 10 0" VerticalAlignment="Center"
                  Style="{StaticResource textBoxSearch}" 
                  Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                  />


        </StackPanel>

        <!-- Таблица с данными -->
        <DataGrid x:Name="dataGrid" Style="{DynamicResource DataGridStyle}" 
               CellStyle="{DynamicResource DataGridCellStyle}" 
               RowStyle="{DynamicResource DataGridRowStyle}" 
               ColumnHeaderStyle="{StaticResource TransparentHeaderStyle}"
               Grid.Row="3" ItemsSource="{Binding FilteredClients}"
                     CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Почта" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Питомец" Binding="{Binding PetName}" Width="*"/>
                <DataGridTextColumn Header="Порода" Binding="{Binding Breed}" Width="*"/>
                <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="Поиск:" HorizontalAlignment="Left" Margin="20,33,0,0" Grid.Row="1" VerticalAlignment="Top" Grid.RowSpan="2"/>
    </Grid>
</UserControl>

