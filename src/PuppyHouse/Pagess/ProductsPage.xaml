<UserControl x:Class="KP_4_PuppyHouse1.Pagess.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:Inter="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <!-- Заголовок -->
            <RowDefinition Height="auto"/>
            <!-- Панель управления -->
            <RowDefinition Height="auto"/>
            <!-- Фильтры и поиск -->
            <RowDefinition Height="*"/>
            <!-- Таблица с данными -->
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Товары" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
                   HorizontalAlignment="Left" Foreground="#121518"/>

        <!-- Панель управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button x:Name="AddProduct_Btn" Margin="10,0,0,0" Width="80" Style="{StaticResource addButton}" Command="{Binding AddProductCommand}" Click="AddProduct_Btn_Click">
                <StackPanel Orientation="Horizontal">

                    <TextBlock Text="➕" FontSize="18"/>
                </StackPanel>
            </Button>

            <Button x:Name="EditTovar_Btn" Margin="10,0,0,0" Style="{StaticResource addButton3}" Command="{Binding DeleteSelectedCommand}" Width="80" Height="35" Click="EditTovar_Btn_Click">
                <StackPanel Orientation="Horizontal">

                    <TextBlock Text="🖉" FontSize="20"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Фильтры и поиск -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 15 0 0" HorizontalAlignment="Stretch">
            <!-- Поле для поиска -->
            <TextBox x:Name="SearchBox" Width="200" Margin="10 0 10 0" VerticalAlignment="Center"
                     Style="{StaticResource textBoxSearch}" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Height="33" 
                     />

            <!-- Сортировка по категории -->
            <ComboBox x:Name="CategoryFilter" Width="170" Margin="10 0 10 0" VerticalAlignment="Center"
                      ItemsSource="{Binding Categories}" 
                      SelectedItem="{Binding SelectedCategory, UpdateSourceTrigger=PropertyChanged}" 
                      Style="{StaticResource ComboBoxFlatStyle}" Height="33" DisplayMemberPath="Name" SelectionChanged="CategoryFilter_SelectionChanged_1"/>

            <!-- Сортировка по бренду -->
            <ComboBox x:Name="BrandFilter" Width="170" Margin="10 0 10 0" VerticalAlignment="Center"
                      ItemsSource="{Binding Brands}" 
                      SelectedItem="{Binding SelectedBrand, UpdateSourceTrigger=PropertyChanged}" 
                      Style="{StaticResource ComboBoxFlatStyle}" Height="33" DisplayMemberPath="Name" SelectionChanged="BrandFilter_SelectionChanged_1"/>

            <!-- Сортировка по стране -->
            <ComboBox x:Name="CountryFilter" Width="170" Margin="10 0 10 0" VerticalAlignment="Center"
                      ItemsSource="{Binding Countries}" 
                      SelectedItem="{Binding SelectedCountry, UpdateSourceTrigger=PropertyChanged}" 
                      Style="{StaticResource ComboBoxFlatStyle}" Height="33" DisplayMemberPath="Name" SelectionChanged="CountryFilter_SelectionChanged_1"
                      />
        </StackPanel>

        <!-- Таблица с данными -->
        <DataGrid x:Name="dataGridTovar" Style="{DynamicResource DataGridStyle}" 
                  CellStyle="{DynamicResource DataGridCellStyle}" 
                  RowStyle="{DynamicResource DataGridRowStyle}" 
                  ColumnHeaderStyle="{StaticResource TransparentHeaderStyle}"
                  Grid.Row="3" ItemsSource="{Binding FilteredProducts}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Товар" IsReadOnly="True" Width="*" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Описание" IsReadOnly="True" Width="2*" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Цена" IsReadOnly="True" Width="*" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="Категория" IsReadOnly="True" Width="*" Binding="{Binding CategoryTovar.Name}"/>
                <DataGridTextColumn Header="Бренд" IsReadOnly="True" Width="*" Binding="{Binding Brand.Name}"/>
                <DataGridTextColumn Header="Страна" IsReadOnly="True" Width="*" Binding="{Binding Country.Name}"/>
                <DataGridTextColumn Header="Вес" IsReadOnly="True" Width="*" Binding="{Binding Size}"/>
                <!-- Столбец с изображением -->
                <DataGridTemplateColumn Header="Фото" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding PhotoFull}" Width="30" Height="30" Stretch="UniformToFill"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
        <Label Content="Категории:" HorizontalAlignment="Left" Margin="232,29,0,0" Grid.Row="1" VerticalAlignment="Top" Grid.RowSpan="2"/>
        <Label Content="Бренд:" HorizontalAlignment="Left" Margin="428,29,0,0" Grid.Row="1" VerticalAlignment="Top" Grid.RowSpan="2"/>
        <Label Content="Поиск:" HorizontalAlignment="Left" Margin="22,29,0,0" Grid.Row="1" VerticalAlignment="Top" Grid.RowSpan="2"/>
        <Label Content="Страны:" HorizontalAlignment="Left" Margin="614,29,0,0" Grid.Row="1" VerticalAlignment="Top" Grid.RowSpan="2"/>
    </Grid>
</UserControl>

