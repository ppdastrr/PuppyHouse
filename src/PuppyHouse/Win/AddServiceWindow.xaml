<Window x:Class="KP_4_PuppyHouse1.Win.AddServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Добавление услуги" Height="640" Width="450"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <Style TargetType="TextBox" x:Key="textBoxInput">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="0 4 0 10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#ebdfe1 "/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border 
        x:Name="Border"
        Background="{TemplateBinding Background}" 
        BorderBrush="{TemplateBinding BorderBrush}" 
        BorderThickness="1"
        CornerRadius="10">
                <Grid>
                    <Path 
                x:Name="Arrow" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Data="M 0 0 L 4 4 L 8 0 Z" 
                Fill="Black" 
                Margin="0,0,8,0" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#D8E6FA" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#B6D3F5" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Arrow" Property="Fill" Value="#555555" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="ComboBox" x:Key="comboBoxInput">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Закруглённый фон -->
                            <Border 
                        x:Name="Border"
                        CornerRadius="10"
                        Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <!-- Стрелка -->
                                    <ToggleButton 
                                Name="ToggleButton" 
                                Grid.Column="2"
                                Focusable="false"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press"
                                Background="#ebdfe1 "
                                BorderThickness="0">
                                        <Path 
                                    Data="M 0 0 L 4 4 L 8 0 Z"
                                    Fill="#888888"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Margin="0,0,8,0"/>
                                    </ToggleButton>
                                    <!-- Текущий элемент -->
                                    <ContentPresenter
                                Name="ContentSite"
                                IsHitTestVisible="False"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                Margin="10,3,25,3"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left" />
                                </Grid>
                            </Border>
                            <!-- Выпадающее меню -->
                            <Popup 
                        Name="Popup"
                        Placement="Bottom"
                        IsOpen="{TemplateBinding IsDropDownOpen}"
                        AllowsTransparency="True"
                        Focusable="False"
                        PopupAnimation="Slide">
                                <Border 
                            x:Name="DropDownBorder"
                            Background="#FFFFFF"
                            BorderBrush="#888888"
                            BorderThickness="1"
                            CornerRadius="10">
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- Условие: если нет элементов -->
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="Height" Value="30"/>
                            </Trigger>
                            <!-- Условие: если ComboBox отключён -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#EEEEEE" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#AAAAAA" />
                            </Trigger>
                            <!-- Условие: если раскрыт список -->
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F0F4F8" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#888888" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- Параметры стиля -->
            <Setter Property="Margin" Value="0 4 0 10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
        </Style>
        <Style TargetType="Button" x:Key="addButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#b7cede "/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Border Background="#EBEAF5">
        <Grid Background="#FFFAFCFB">
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="EditAddTxt" Grid.Row="0" FontSize="25" FontWeight="SemiBold" Margin="30 35 0 0"
                        HorizontalAlignment="Left" Foreground="#121518" Text="Добавление услуги"/>

            <!--Separator-->
            <Separator Grid.Row="1"  Height="8" Background="#d0e1e8" Margin="0 10 0 10" />

            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="30,0,30,0">
                <TextBox x:Name="IDTxt"  Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" Visibility="Collapsed" />
                <TextBlock FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" Text="Наименование" FontSize="14"/>
                <TextBox x:Name="NameTxt"  Style="{StaticResource textBoxInput}" Height="63" TextBlock.FontSize="14" />

                <TextBlock Text="Описание" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <TextBox x:Name="DescTxt"  Style="{StaticResource textBoxInput}" AcceptsReturn="True" TextWrapping="Wrap" Height="114" TextBlock.FontSize="14" />

                <TextBlock Text="Категория услуги" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <ComboBox DisplayMemberPath="Name" x:Name="CategoryCB"   Style="{StaticResource ComboBoxFlatStyle}" Height="30" TextBlock.FontSize="14" />

                <TextBlock Text="Цена" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <TextBox x:Name="PriceTxt" Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" />

                <TextBlock Text="Длительность (в минутах)" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <TextBox x:Name="TimeTxt"  Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" />
            </StackPanel>

            <Separator Grid.Row="3" Height="8" Background="#d0e1e8" Margin="0 10 0 10" />

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" >
                <Button x:Name="SaveBtn" Style="{StaticResource addButton}" Margin="10 0 0 15" Click="AddBtn_Click" Width="188" Height="36" Background="#aec289" FontWeight="Bold" Content="Добавить услугу">

                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>


