<Window x:Class="KP_4_PuppyHouse1.Win.AddPet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Добавление животного" Height="450" Width="450"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <Style TargetType="TextBox" x:Key="textBoxInput">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="0 4 0 10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#f4f2ef "/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="Button" x:Key="addButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#aec289"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Border Background="#EBEAF5">
        <Grid Background="#FFFAFCFB">
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" FontSize="25" FontWeight="SemiBold" Margin="30 35 0 0"
                        HorizontalAlignment="Left" Foreground="#121518" Text="Добавление животного"/>

            <!--Separator-->
            <Separator Grid.Row="1"  Height="8" Background="#d0e1e8" Margin="0 10 0 10" />

            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="30,0,30,0">
                <TextBlock FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" Text="Кличка" FontSize="14"/>
                <TextBox x:Name="PetNameTextBox" Text="{Binding PetName}" Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" />

                <TextBlock Text="Порода" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <TextBox x:Name="PetPorodaTextBox" Text="{Binding Poroda}" Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" />

                <TextBlock Text="Возраст" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <TextBox x:Name="PetAgeTextBox" Text="{Binding PetAge}" Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" />
            </StackPanel>

            <Separator Grid.Row="3" Height="8" Background="#d0e1e8" Margin="0 10 0 10" />

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" >
                <Button x:Name="AddPetButton" Style="{StaticResource addButton}" Margin="10 0 0 15" Command="{Binding SavePetCommand}" Width="179" Height="36" FontWeight="Bold" Click="AddPetButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Добавить животное"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
