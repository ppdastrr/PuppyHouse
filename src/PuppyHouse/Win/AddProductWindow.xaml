<Window x:Class="KP_4_PuppyHouse1.Win.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Добавление товара" Height="810" Width="800"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <Style TargetType="TextBox" x:Key="textBoxInput">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="0 4 0 10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#ebdfe1 "/>
            <Setter Property="BorderBrush" Value="#dfb9b6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border 
        x:Name="Border"
        Background="{TemplateBinding Background}" 
        BorderBrush="{TemplateBinding BorderBrush}" 
        BorderThickness="1"
        CornerRadius="10">
                <Grid>
                    <Path 
                x:Name="Arrow" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Data="M 0 0 L 4 4 L 8 0 Z" 
                Fill="Black" 
                Margin="0,0,8,0" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#D8E6FA" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#B6D3F5" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Arrow" Property="Fill" Value="#555555" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>


        <Style TargetType="Button" x:Key="addButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#aec289 "/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Border Background="#EBEAF5">
        <Grid Background="#FFFAFCFB">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" MinHeight="562" />
                <RowDefinition Height="auto" MinHeight="22.68" />
                <RowDefinition Height="auto" MinHeight="51" />


            </Grid.RowDefinitions>

            <TextBlock x:Name="EditAddTxt" Grid.Row="0" Grid.ColumnSpan="2" FontSize="25" FontWeight="SemiBold" Margin="10,18,0,18"
                        HorizontalAlignment="Left" Foreground="#121518" Text="Добавление товара" Width="390"/>

            <!--Separator-->
            <Separator Grid.Row="1" Grid.ColumnSpan="2" Background="#d0e1e8" Margin="0,10,0,10" />

            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Vertical" Margin="30,0,30,51" Grid.RowSpan="3">
                <TextBlock FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" Text="Наименование" FontSize="14"/>
                <TextBox x:Name="NameTxt" TextWrapping="Wrap" Style="{StaticResource textBoxInput}" Height="72" TextBlock.FontSize="14" />
                <TextBox x:Name="IDTxt"  Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" Visibility="Collapsed" />
                <TextBlock Text="Описание" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <TextBox x:Name="DescTxt"  Style="{StaticResource textBoxInput}" AcceptsReturn="True" TextWrapping="Wrap" Height="117" TextBlock.FontSize="14" />

                <TextBlock Text="Цена" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <TextBox x:Name="PriceTxt" Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" />
                <TextBlock Text="Вес" FontWeight="Medium" Foreground="#696969" FontSize="14"/>
                <TextBox x:Name="WeightTxt" Style="{StaticResource textBoxInput}" Height="30" TextBlock.FontSize="14" />

                <TextBlock Text="Категория" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <ComboBox x:Name="CategoryCB" DisplayMemberPath="Name" Style="{StaticResource ComboBoxFlatStyle}" Height="30" BorderBrush="#FFEC9F9F" TextBlock.FontSize="14" />

                <TextBlock Text="Бренд" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <ComboBox x:Name="BrendCB" DisplayMemberPath="Name"  Style="{StaticResource ComboBoxFlatStyle}" Height="30" BorderBrush="#FFEC9F9F" TextBlock.FontSize="14" />

                <TextBlock Text="Страна производства" FontWeight="Medium" Foreground="#696969" Margin="0,0,0,1" FontSize="14"/>
                <ComboBox x:Name="CountryCB" DisplayMemberPath="Name"  Style="{StaticResource ComboBoxFlatStyle}" Height="30" BorderBrush="#FFEC9F9F" TextBlock.FontSize="14" />


            </StackPanel>

            <!-- Большое изображение справа -->
            <Image x:Name="LargeProductImage" Grid.Row="2" Grid.Column="1"  Stretch="UniformToFill" Margin="10,10,25,10"/>

            <Separator Grid.Row="3" Grid.ColumnSpan="2" Background="#d0e1e8" Margin="0,10,0,10" />


            <Button x:Name="SaveBtn"  Style="{StaticResource addButton}" Margin="18,25,0,0"  Width="231" Height="51"  FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Left" Click="AddBtn_Click" Content="Добавить товар" Grid.Row="4" Grid.Column="1" FontSize="18"/>
            <Button x:Name="PhotoBtn"  Style="{StaticResource addButton}" Margin="30,548,0,0"  Width="160" Height="36" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Left" Click="PhotoBtn_Click" Content="Добавить фото" Grid.Row="2" Background="#FFDFB9B6" Grid.RowSpan="2"/>

        </Grid>
    </Border>
</Window>